<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pearadmin.modules.chat.mapper.TChatMessageMapper">

    <resultMap type="TChatMessage" id="TChatMessageResult">
        <result property="msgId" column="msg_id"/>
        <result property="sessionId" column="session_id"/>
        <result property="role" column="role"/>
        <result property="msgType" column="msg_type"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectTChatMessageList" parameterType="TChatMessage" resultMap="TChatMessageResult">
        select `msg_id`,`session_id`,`role`,`msg_type`,`content`,`create_time` from `t_chat_message`
        <where>
             <if test="sessionId != null  and sessionId != ''"> and `session_id` = #{sessionId}</if>
             <if test="role != null  and role != ''"> and `role` = #{role}</if>
             <if test="msgType != null  and msgType != ''"> and `msg_type` = #{msgType}</if>
             <if test="content != null  and content != ''"> and `content` = #{content}</if>
        </where>
    </select>

</mapper>
